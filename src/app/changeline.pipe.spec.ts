import { ChangelinePipe } from './changeline.pipe';

describe('ChangelinePipe', () => {
  it('create an instance', () => {
    const pipe = new ChangelinePipe();
    expect(pipe).toBeTruthy();
  });
});
